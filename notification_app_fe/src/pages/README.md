This folder is intentionally kept for submission structure parity with the evaluation brief.

Next.js treats `src/pages` as a routing directory, so the actual page-level UI modules live in `src/page-modules` to avoid router conflicts with `src/app`.
